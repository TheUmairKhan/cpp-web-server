# Configure port and routes
port 80;

route / {
    handler echo;
}

route /echo {
    handler echo;
}

route /static {
    handler static;
    root /var/www/static;
}

route /public {
    handler static;
    root /var/www/static;
}